# ============================================
# NuxtHub Database
# ============================================
# NuxtHub auto-detects database:
# - If DATABASE_URL is set → uses postgres-js driver (Docker PostgreSQL)
# - If not set → uses PGlite (embedded, zero-config)
#
# For local development with Docker:
# 1. Start PostgreSQL: pnpm docker:up (from root)
# 2. Set DATABASE_URL below
# 3. Run migrations: pnpm db:migrate

# Option A: Docker PostgreSQL (recommended for team development)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/multi_vendor

# Option B: PGlite (embedded, no Docker needed)
# Leave DATABASE_URL empty to use PGlite for local dev

# ============================================
# Authentication (nuxt-auth-utils)
# ============================================
# Generate with: openssl rand -base64 32
NUXT_AUTH_SECRET=your-secret-key-here-min-32-characters-long
NUXT_AUTH_ORIGIN=http://localhost:3000

# ============================================
# Nuxt Hub (production only)
# ============================================
# These are auto-set by NuxtHub CLI during deployment
NUXT_HUB_PROJECT_KEY=
NUXT_HUB_ENV=

# ============================================
# Electric SQL Sync
# ============================================
# Local development (Docker)
ELECTRIC_URL=http://localhost:30000

# Production (Electric Cloud or self-hosted)
# ELECTRIC_URL=https://your-electric-instance.com
# ELECTRIC_API_KEY=your-api-key

# ============================================
# PGlite (client-side local database)
# ============================================
# Name of the IndexedDB database in browser
PGLITE_DB_NAME=multi_vendor_local
